A very lightweight framework for helping to use tensorflow.

Only test on tensorflow r1.1

## Python2
For python2, replace `fullmatch` in utils/string_utils.py with `match`, and add '^''$' at the begin and end of regex
